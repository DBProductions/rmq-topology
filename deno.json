{
  "tasks": {
    "lint": "deno lint --fix",
    "format": "deno fmt --check",
    "test": "vitest run",
    "dev": "deno run --watch main.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
