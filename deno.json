{
  "tasks": {
    "lint": "deno lint --fix",
    "format": "deno fmt",
    "test": "vitest run",
    "bundle": "rollup -c",
    "dev": "deno run --watch main.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
