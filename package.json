{
  "name": "rmq-topology",
  "version": "0.0.3",
  "description": "A small tool to simulate RabbitMQ topologies and see the message flow.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run src --coverage",
    "start:cypress": "npx cypress open",
    "run:cypress": "npx cypress run --headless --config video=false -b chrome",
    "lint": "npx eslint ./src",
    "lint:fix": "npx eslint ./src --fix",
    "format": "npx prettier --write \"{,!(node_modules)/**/}*.{js,ts}\"",
    "bundle": "npx rollup --config rollup.config.mjs",
    "release": "npm run format && npm run lint && npm test && npm run bundle && (npm run start & npx wait-on http://localhost:3000 && npm run run:cypress); pkill -f 'node server/index.js' || true",
    "start": "node server/index.js"
  },
  "keywords": [
    "rabbitmq",
    "topology",
    "rollup",
    "eslint",
    "prettier",
    "jsdoc"
  ],
  "author": "DBProductions",
  "repository": {
    "type": "git",
    "url": "git://github.com/DBProductions/rmq-topology"
  },
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-istanbul": "^5.0.0",
    "clean-jsdoc-theme": "^5.1.1",
    "cypress": "^16.0.0",
    "eslint": "^10.9.1",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "rollup-plugin-jsdoc": "^0.1.2",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
